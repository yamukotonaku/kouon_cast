# AI仏教説話自動生成・Podcast配信システム
# Python 3.10+

# Google Gen AI SDK (Gemini 2.5 Pro)
google-genai>=1.0.0

# 環境変数 (.env)
python-dotenv>=1.0.0

# Podcast RSS 生成
podgen>=1.1.0

# VOICEVOX API 呼び出し
requests>=2.28.0

# WAV 結合・MP3 変換（MP3 出力には ffmpeg のインストールが必要）
pydub>=0.25.0

# BGM 手続き生成（numpy + scipy でフィルタ・波形）
# MusicGen 利用時は PyTorch と NumPy 1.x が必要なため、numpy は 2 未満に固定
numpy>=1.24.0,<2
scipy>=1.10.0

# BGM を MusicGen で生成する場合（--bgm-style musicgen）に必要
transformers>=4.36.0
torch
accelerate
